'use strict';

describe('myApp.topay module', function() {

  beforeEach(module('myApp.topay'));

  describe('topay controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});